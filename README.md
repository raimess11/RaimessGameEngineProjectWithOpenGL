# RaimessGameEngineProjectWithOpenGL
Built a custom game engine to learn engine architecture fundamentals. Implemented a basic render pipeline using OpenGL, a game logic container, and an event-handling system. Currently capable of rendering background frames with core engine systems established.
